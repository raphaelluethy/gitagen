import { ipcMain } from "electron";

export function registerEventsHandlers(): void {
	// Placeholder for future event subscriptions (fs watch, repo updates)
	// Renderer can use events.onRepoUpdated, events.onRepoError, events.onConflictDetected
}
